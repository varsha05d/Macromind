<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Gallery access: Android 12 and below -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Gallery access: Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Camera is required to install this app -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="false"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MacroMind"
        android:usesCleartextTraffic="false">

        <!-- Splash screen (new launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MacroMind.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home screen (no longer launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Camera screen -->
        <activity
            android:name=".CameraActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Crop + scan screen -->
        <activity
            android:name=".CropActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Results screen -->
        <activity
            android:name=".ResultsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Scan history screen -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Debug/test activity — not a launcher -->
        <activity
            android:name=".AssetTestActivity"
            android:exported="false" />

        <!-- FileProvider for sharing result images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.macromind.foodscanner.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
